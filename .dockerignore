node_modules
.next
.git
.github
coverage
# The CDK app isn't part of either Docker image, and cdk.out lives inside
# the same repo root used as the Docker build context — without this,
# CDK's own asset-staging copy recurses into its own output directory.
infra/cdk.out
infra/node_modules
*.md
!README.md
**/*.test.ts
**/*.test.tsx
src/test
vitest.config.mts
.env*
.DS_Store
npm-debug.log*
