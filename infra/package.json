{
  "name": "yoto-downloader-infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy --all --require-approval never"
  },
  "devDependencies": {
    "@types/node": "^22",
    "aws-cdk": "2.1135.1",
    "ts-node": "^10.9.2",
    "typescript": "^5"
  },
  "dependencies": {
    "aws-cdk-lib": "2.264.0",
    "constructs": "^10.8.1"
  }
}
